const { Controller, sequelize } = require("../models/index");
const { Op } = require("sequelize");

class Controller 
static async addListener(req, res) {
   const showGames = await.Games
   try {

   } catch (error) {
      console.log(error.message);

   }
}


module.exports = Controller