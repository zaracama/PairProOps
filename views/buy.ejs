<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: #f8f9fa; /* Light gray background color */
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      /* Center the content */
      .container {
        max-width: 600px;
        margin: 50px auto;
        background-color: #fff; /* White background color */
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      h1 {
        color: #007bff; /* Blue color */
      }

      img {
        width: 50%;
        height: auto;
        border-radius: 8px;
        margin: 20px auto; /* Center the image horizontally */
        display: block;
      }

      form {
        text-align: center;
      }

      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
      }

      input,
      select {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        border: 1px solid #ced4da; /* Bootstrap's gray border color */
        border-radius: 4px;
        box-sizing: border-box;
      }

      button {
        background-color: #28a745; /* Bootstrap's success green color */
        color: #fff;
        padding: 10px 15px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      button:hover {
        background-color: #218838; /* Darker green color on hover */
      }
    </style>
  </head>
  <body>
    <%- include('../partials/_navbar') %>

    <div class="container">
      <h1>Buy Now</h1>

      <div>
        <img src="<%= carData.carImage %>" alt="Car Picture" />
        <h3><%= carData.title %></h3>
      </div>

      <div>
        <form action="http://localhost:3000/cars/buy/<%= carData.id %>" method="post">
          <input style="visibility: hidden" type="number" name="UserId" value="<%= userData.id %>" />
          <input style="visibility: hidden" type="number" name="CarId" value="<%= carData.id %>" /><br />

          <label for="totalAmount">Total After Tax Amount</label><br />
          <input type="number" name="totalAmount" value="<%= carData.afterTax %>" /><br /><br />

          <label for="shippingAddress">Shipping Address</label><br />
          <input type="text" name="shippingAddress" value="<%= userData.UserProfile.address %>" /><br /><br />

          <label for="paymentMethod">Payment Method</label><br />
          <select name="paymentMethod">
            <option value="" selected disabled>--Select Payment Method--</option>
            <option value="Cash">Cash</option>
            <option value="Credit">Credit</option></select
          ><br /><br />

          <button type="submit">Buy Car</button>
        </form>
      </div>
    </div>
  </body>
</html>